# Core data manipulation
pandas
numpy

# Plotting and visualization
matplotlib

# If you're using Anaconda, these come pre-installed. But it's still good practice to list them so others know what's needed.

# You can install missing packages with:
# pip install -r requirements.txt (do not put "#" before pip)

# HAPPY CODING!
